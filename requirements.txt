# Core simulation
mujoco>=3.1
numpy>=1.24
scipy>=1.10

# Inverse kinematics (mink is primary; a DLS fallback in control/ik.py needs no extra deps)
mink>=0.0.4

# LLM planner (Google Gemini)
google-genai>=0.3
pydantic>=2.5
python-dotenv>=1.0

# Data logging / config / media
h5py>=3.8
pyyaml>=6.0
matplotlib>=3.7
imageio>=2.31
imageio-ffmpeg>=0.4

# Dev (also available via: pip install -e ".[dev]")
pytest>=7.4
ruff>=0.5

# NOTE: these are version floors. After the first successful install, pin exact
# versions with `pip freeze > requirements.lock.txt`.
